# Contract status

This distribution intentionally exposes a compatibility boundary rather than claiming feature parity with Google's complete iOS GenAI binaries.

The machine-readable manifest and exported C capability functions are the normative contract. `GENAI_CAPABILITIES.md` describes how consumers should use them; `GENAI_CAPABILITY_SCHEMA.md` defines schema evolution rules.
